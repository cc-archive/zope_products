<h1 tal:replace="structure here/manage_page_header">Header</h1>
<h2 tal:define="manage_tabs_message options/manage_tabs_message | nothing"
    tal:replace="structure here/manage_tabs">Tabs</h2>

<h3> <code>Sitemap Tool</code> </h3>

<p>This tool allows site administrators to create a virtual navigation
structure for their site.  The tool provides an interface for editting the
navigational structure, along
with methods for rendering all or part of the site map.</p>

<h1 tal:replace="structure here/manage_page_footer">Footer</h1>